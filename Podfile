platform :ios, '11.2'

target 'MTG Card Tracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MTG Card Tracker

  target 'MTG Card TrackerTests' do
    inherit! :search_paths

    # Pods for testing
  end

  target 'MTG Card TrackerUITests' do
    inherit! :search_paths

    # Pods for testing
    pod 'XCTest-Gherkin'
  end

end
